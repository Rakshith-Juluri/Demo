import React from 'react';

// Header removed per request; this component intentionally renders nothing
// to preserve imports/usage while removing the header UI.
export default function FundHeader() {
  return null;
}
