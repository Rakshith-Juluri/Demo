import React from 'react';

export default function AddBeneficiaryModal({ showAddBeneficiary, setShowAddBeneficiary, newBen, setNewBen, addBeneficiary }) {
  if (!showAddBeneficiary) return null;
  return (
   <>
   
   </>
  );
}
